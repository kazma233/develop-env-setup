version: '3.1'

services:
  v2ray:
    image: v2ray/official
    restart: always
    ports:
      - 23333:23333
    volumes:
      - ./config.json:/etc/v2ray/config.json
    command: v2ray --config=/etc/v2ray/config.json
    
